{
  "name": "janitor",
  "version": "1.0.0",
  "description": "Clean up the web.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^1.0.1"
  }
}
